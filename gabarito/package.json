{
  "name": "ace8-projeto-base",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Projeto base full-stack do minicurso ACE 8 — Testes Automatizados",
  "scripts": {
    "postinstall": "npm --prefix server install && npm --prefix client install",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run dev",
    "dev": "concurrently -n server,client -c blue,green \"npm run dev:server\" \"npm run dev:client\"",
    "test": "npm --prefix server test",
    "coverage": "npm --prefix server run coverage",
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "test:e2e": "start-server-and-test dev http://localhost:5173 cy:run"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "cypress": "^13.17.0",
    "start-server-and-test": "^2.0.9"
  }
}
